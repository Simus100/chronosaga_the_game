# Local model artifacts

Do not commit GGUF model weights to the normal Git repository.

The default offline model is a hard product requirement, but its exact family, quantization and runtime budget are selected only after the P0 benchmark.

See:
- `docs/PLATFORM_DISTRIBUTION_LOCAL_AI_FEASIBILITY_v1.md`
- future `docs/P0_BENCHMARK_PLAN_v0.1.md`
