# Desktop shell

This is the Tauri v2 scaffold for the Windows Full Offline Edition.

P0 additions still required:

- SQLite adapter;
- bundled `llama-server.exe` sidecar;
- selected GGUF model;
- local AI lifecycle management;
- Windows installer validation.

Those items are intentionally gated by `docs/PLATFORM_DISTRIBUTION_LOCAL_AI_FEASIBILITY_v1.md`.
